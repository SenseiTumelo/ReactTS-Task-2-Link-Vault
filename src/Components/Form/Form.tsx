import { Overlay } from "../Overlay/Overlay"

export const Form = () => {
  return (
    <Overlay>
        <div className="form-card">
        <form action="">
            <input type="text" />
        </form>
        </div>
    </Overlay>
  )
}
